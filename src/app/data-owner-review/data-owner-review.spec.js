(function () {
    'use strict';

    describe('cmDataOwnerReview module', function () {

        beforeEach(module('cmDataOwnerReview'));

        describe('DataOwnerReview controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var data_owner_reviewCtrl = $controller('DataOwnerReview');
                expect(data_owner_reviewCtrl).toBeDefined();
            }));

        });
    });

})();
