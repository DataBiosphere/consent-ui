(function () {
    'use strict';

    angular.module('cmHomeHelp')
        .controller('HomeHelp', HomeHelp);

    /* ngInject */
    function HomeHelp($scope, $rootScope) {

        init();

        function init() {
        }
    }
})();
