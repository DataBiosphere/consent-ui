(function () {
    'use strict';

    describe('cmInvalidRestrictions module', function () {

        beforeEach(module('cmInvalidRestrictions'));

        describe('InvalidRestrictions controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var invalid_restrictionsCtrl = $controller('InvalidRestrictions');
                expect(invalid_restrictionsCtrl).toBeDefined();
            }));

        });
    });

})();
