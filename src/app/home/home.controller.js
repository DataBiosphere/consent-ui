(function () {
    'use strict';

    angular.module('cmHome')
        .controller('Home', Home);

    /* ngInject */
    function Home($scope, $rootScope) {

        init();

        function init() {
        }
    }
})();
