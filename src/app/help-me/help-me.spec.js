(function () {
    'use strict';

    describe('cmHelpUs module', function () {

        beforeEach(module('cmHelpUs'));

        describe('HelpUs controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var help_usCtrl = $controller('HelpUs');
                expect(help_usCtrl).toBeDefined();
            }));

        });
    });

})();
